describe("share-week-with-email", () => {
    it("should call the single week sharing when sharing only one week", () => {
        // given a payload with a shareAllWeeks attribute as false
        const payload = {
            email: "irrelevent",
            shareAllWeeks: false,
        };
    })
})
